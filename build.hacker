// virus
// cargo

> cd source-code
>>> cd compiler && cargo build --release
>>> cd virus && virus build --production
>>> cd runtime && virus build --production
>>> cd installer && virus build --production
>>> cd vm && virus build --production
>>> cd code && virus build --production
